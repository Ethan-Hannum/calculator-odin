const num1 = null;
const num2 = null;
const operator = null;

function add(x, y) {
    return x + y;
};

function subtract(x, y) {
    return x - y;
};

function multiply(x, y) {
    return x * y;
};

function divide(numer, denom) {
    return numer / denom;
};